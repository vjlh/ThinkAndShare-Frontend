import { OnChanges, SimpleChanges, AfterViewInit, ViewContainerRef } from '@angular/core';
export declare class CollapsibleDirective implements OnChanges, AfterViewInit {
    private viewContainer;
    accordion: false;
    selected: string[];
    openHeaderClass: string;
    openContentClass: string;
    collapsedHeaderClass: string;
    collapsedContentClass: string;
    private headers;
    private contents;
    constructor(viewContainer: ViewContainerRef);
    ngOnChanges(changes: SimpleChanges): void;
    ngAfterViewInit(): void;
    private toggleCollapsible(key);
    private toggleAccordion;
    private updateView();
    private toggleClasses;
}
